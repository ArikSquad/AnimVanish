language: java
jdk:
  - openjdk17
 cache:
  directories:
    - $HOME/.m2/
script:
  - mvn clean package
deploy:
  provider: dev
  api_key: "${{ secrets.GITHUB_TOKEN }}"
  file: "target/animvanish-1.0.8.jar"
  skip_cleanup: true
  on:
   tags: true
